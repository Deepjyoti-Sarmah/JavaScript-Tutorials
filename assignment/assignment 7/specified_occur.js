// 6. What string method can be used to check the occurrence of a specified
// text in a string?

const check = (str) => {
    let b = str.includes("s");
    return b;
}

console.log( check("soham"));